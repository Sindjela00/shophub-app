{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5433;Database=shophub;Username=shophub;Password=shophub"
  },
  "Jwt": {
    "_comment": "Dev-only signing key — never reuse this value outside local development.",
    "SigningKey": "dev-only-signing-key-do-not-use-in-production-0123456789abcdef"
  },
  "Grafana": {
    "_comment": "Dev-only — points at a `kubectl port-forward svc/shophub-grafana 3300:80 -n shophub` tunnel. AdminPassword and UsersOrgServiceAccountToken are NOT set here — run `dotnet user-secrets set Grafana:AdminPassword ...` / `Grafana:UsersOrgServiceAccountToken ...` per shophub-kube-state/BOOTSTRAP.md, never commit them.",
    "BaseUrl": "http://localhost:3300",
    "AdminUser": "admin"
  }
}
